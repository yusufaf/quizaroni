{
	"name": "@quizaroni/shared-types",
	"version": "0.0.0",
	"private": true,
	"description": "Internal domain types shared by @quizaroni/web and @quizaroni/api. Never published.",
	"license": "MIT",
	"types": "./src/index.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"test": "echo \"(types-only package; build is the check)\" && exit 0"
	},
	"devDependencies": {
		"typescript": "^5.7.3"
	}
}
