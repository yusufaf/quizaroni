import { useCallback, useEffect, useState } from 'react';
import { UUID } from 'shared/types';

type UseActionsStackProps = {};

const useActionsStack = (props: UseActionsStackProps) => {
    const {} = props;

    return {};
};

export default useActionsStack;
