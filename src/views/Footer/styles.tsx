import { styled } from "@mui/system";

export const StyledFooter = styled("footer")({
    display: "flex",
    alignItems: "center",
    bottom: "0",
    height: "4rem",
    width: "100%",
    marginTop: "auto",
})
